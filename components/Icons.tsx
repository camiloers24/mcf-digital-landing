'use client'
// Phosphor v2: canonical exports use the Icon suffix (e.g. CpuIcon).
// We import those and re-export under clean names so page files stay readable.
import {
  ChatTextIcon, TerminalIcon, CaretDownIcon,
  FlameIcon, TrendDownIcon, HardDrivesIcon, VaultIcon,
  FireIcon, GasPumpIcon, PackageIcon, FileTextIcon,
  LightningIcon, DropIcon, PlugsConnectedIcon, CpuIcon,
  MonitorIcon, SpeakerSimpleSlashIcon,
  CurrencyDollarIcon, HouseIcon, UsersThreeIcon, ChartLineUpIcon,
  CheckCircleIcon, ShieldCheckIcon, CertificateIcon,
  WarningIcon, ArrowsLeftRightIcon, BriefcaseIcon,
  // Miners page
  WrenchIcon, ThermometerIcon, CodeIcon, ShareNetworkIcon,
  BuildingsIcon, CoinsIcon, StackIcon, ArrowsOutIcon, GearSixIcon,
  // HNWI page
  MagnifyingGlassIcon, ClockIcon, MapPinIcon, ChartLineDownIcon,
  PresentationChartIcon, CalendarIcon,
  // About page
  PhoneCallIcon, HandCoinsIcon, LightbulbIcon,
} from '@phosphor-icons/react'

export {
  ChatTextIcon            as ChatText,
  TerminalIcon            as Terminal,
  CaretDownIcon           as CaretDown,
  FlameIcon               as Flame,
  TrendDownIcon           as TrendDown,
  HardDrivesIcon          as HardDrives,
  VaultIcon               as Vault,
  FireIcon                as Fire,
  GasPumpIcon             as GasPump,
  PackageIcon             as Package,
  FileTextIcon            as FileText,
  LightningIcon           as Lightning,
  DropIcon                as Drop,
  PlugsConnectedIcon      as PlugsConnected,
  CpuIcon                 as Cpu,
  MonitorIcon             as Monitor,
  SpeakerSimpleSlashIcon  as SpeakerSimpleSlash,
  CurrencyDollarIcon      as CurrencyDollar,
  HouseIcon               as House,
  UsersThreeIcon          as UsersThree,
  ChartLineUpIcon         as ChartLineUp,
  CheckCircleIcon         as CheckCircle,
  ShieldCheckIcon         as ShieldCheck,
  CertificateIcon         as Certificate,
  WarningIcon             as Warning,
  ArrowsLeftRightIcon     as ArrowsLeftRight,
  BriefcaseIcon           as Briefcase,
  // Miners page
  WrenchIcon              as Wrench,
  ThermometerIcon         as Thermometer,
  CodeIcon                as Code,
  ShareNetworkIcon        as ShareNetwork,
  BuildingsIcon           as Buildings,
  CoinsIcon               as Coins,
  StackIcon               as Stack,
  ArrowsOutIcon           as ArrowsOut,
  GearSixIcon             as GearSix,
  // HNWI page
  MagnifyingGlassIcon     as MagnifyingGlass,
  ClockIcon               as Clock,
  MapPinIcon              as MapPin,
  ChartLineDownIcon       as ChartLineDown,
  PresentationChartIcon   as PresentationChart,
  CalendarIcon            as Calendar,
  // About page
  PhoneCallIcon           as PhoneCall,
  HandCoinsIcon           as HandCoins,
  LightbulbIcon           as Lightbulb,
}
